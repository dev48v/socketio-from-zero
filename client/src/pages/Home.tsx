// Step 5: placeholder Home so the route compiles. The full grid
// view with live cells, flash effect, and per-row links lands in
// step 6. Splitting that out keeps each commit on a single concept.
export function Home() {
  return (
    <div>
      <h1>Live Stock Ticker</h1>
      <p>Wiring up Socket.io transport in step 6.</p>
    </div>
  );
}
