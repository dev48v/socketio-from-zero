{
  "name": "socketio-from-zero",
  "private": true,
  "version": "1.0.0",
  "description": "Day 28 of TechFromZero — Live Stock Ticker built with Socket.io. Compares Socket.io rooms/ack/fallback against the raw ws library used in Day 27.",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace=server",
    "dev:client": "npm run dev --workspace=client",
    "build:server": "npm run build --workspace=server",
    "build:client": "npm run build --workspace=client"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
